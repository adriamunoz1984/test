(() => {
  const menuButton = document.querySelector('.menu-button'); const nav = document.querySelector('nav[id^="site-nav-"]'); const menuClose = nav?.querySelector('.menu-close'); let returnFocus;
  const closeMenu = (returnFocusToButton = true) => { if (!nav?.classList.contains('open')) return; nav.classList.remove('open'); menuButton?.setAttribute('aria-expanded', 'false'); const label = menuButton?.querySelector('.sr-only'); if (label) label.textContent = 'Open menu'; if (returnFocusToButton) returnFocus?.focus(); };
  const openMenu = () => { returnFocus = document.activeElement; nav?.classList.add('open'); menuButton?.setAttribute('aria-expanded', 'true'); const label = menuButton?.querySelector('.sr-only'); if (label) label.textContent = 'Close menu'; menuClose?.focus(); };
  menuButton?.addEventListener('click', () => nav?.classList.contains('open') ? closeMenu() : openMenu()); menuClose?.addEventListener('click', () => closeMenu()); nav?.querySelectorAll('a').forEach((link) => link.addEventListener('click', () => closeMenu(false)));
  document.addEventListener('keydown', (event) => { if (!nav?.classList.contains('open')) return; if (event.key === 'Escape') { event.preventDefault(); closeMenu(); } if (event.key !== 'Tab') return; const items = [...nav.querySelectorAll('button,a[href]')].filter((item) => !item.disabled); const first = items[0], last = items.at(-1); if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus(); } else if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus(); } });
  const dialog = document.querySelector('.poc-dialog'); const message = dialog?.querySelector('.poc-dialog-message'); const notice = (text) => { if (dialog && message) { message.textContent = text; dialog.showModal(); } }; document.querySelector('.dialog-close')?.addEventListener('click', () => dialog.close()); document.querySelectorAll('[data-notice]').forEach((control) => control.addEventListener('click', () => notice(control.dataset.notice)));
  document.querySelectorAll('.newsletter-poc').forEach((form) => form.addEventListener('submit', (event) => { event.preventDefault(); const email = form.querySelector('input[type="email"]'); const message = form.querySelector('.form-message'); if (!email.validity.valid) { message.textContent = 'Enter a valid email address to join the list.'; message.className = 'form-message error'; email.setAttribute('aria-invalid', 'true'); email.focus(); return; } message.textContent = 'This proof of concept does not submit data.'; message.className = 'form-message'; email.removeAttribute('aria-invalid'); }));
  document.querySelectorAll('[data-gallery]').forEach((gallery) => {
    const buttons = [...gallery.querySelectorAll('.gallery-thumb')]; const panels = [...gallery.querySelectorAll('[data-gallery-panel]')]; const announce = gallery.querySelector('.gallery-announcement');
    buttons.forEach((button, index) => button.addEventListener('click', () => {
      const target = button.dataset.galleryTarget; const kind = button.dataset.galleryKind || 'image';
      panels.forEach((panel) => { const active = panel.dataset.galleryPanel === target; panel.hidden = !active; panel.classList.toggle('is-current', active); if (!active) panel.querySelectorAll('video').forEach((video) => video.pause()); });
      buttons.forEach((item) => { const active = item === button; item.classList.toggle('is-current', active); item.toggleAttribute('aria-current', active); item.querySelector('.sr-only')?.remove(); });
      const current = document.createElement('span'); current.className = 'sr-only'; current.textContent = 'Current media'; button.append(current);
      announce.textContent = `Showing ${kind} ${index + 1} of ${buttons.length}.`;
    }));
  });
})();
