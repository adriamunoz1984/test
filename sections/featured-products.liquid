<section id="{{ section.settings.anchor | default: 'products' | handleize }}" class="products section-dark" aria-labelledby="products-title-{{ section.id }}" {{ section.shopify_attributes }}><div class="section-heading"><div><p class="eyebrow">{{ section.settings.eyebrow | escape }}</p><h2 id="products-title-{{ section.id }}">{{ section.settings.heading | escape }}</h2></div></div><div class="product-grid">{% if section.settings.collection != blank and section.settings.collection.products_count > 0 %}{% for product in section.settings.collection.products limit: section.settings.limit %}{% render 'product-card', product: product, approved_ganja_gallery_product: section.settings.approved_ganja_gallery_product, enable_approved_ganja_gallery: section.settings.enable_approved_ganja_gallery %}{% endfor %}{% else %}<p class="product-empty-state">Configure a featured collection with approved catalog products to preview this area.</p>{% endif %}</div><p class="catalog-note">{{ section.settings.catalog_note | escape }}</p></section>
{% schema %}{"name":"Featured products","limit":1,"settings":[{"type":"text","id":"eyebrow","label":"Eyebrow","default":"CURRENT LINEUP"},{"type":"text","id":"heading","label":"Heading","default":"Featured products"},{"type":"collection","id":"collection","label":"Featured collection"},{"type":"range","id":"limit","label":"Maximum product count","min":1,"max":3,"step":1,"default":3},{"type":"product","id":"approved_ganja_gallery_product","label":"Approved Ganja Gadget gallery product","info":"Select only after confirming its five media items and order against CATALOG.md."},{"type":"checkbox","id":"enable_approved_ganja_gallery","label":"Enable approved five-image Ganja Gadget gallery","default":false,"info":"Enable only after the selected product is verified to contain the five approved media items in the approved order."},{"type":"text","id":"catalog_note","label":"Catalog note","default":"Product details and checkout are not connected in this proof of concept."},{"type":"text","id":"anchor","label":"Section anchor","default":"products"}],"presets":[{"name":"Featured products"}]}{% endschema %}
